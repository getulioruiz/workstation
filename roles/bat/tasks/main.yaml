---
- name: Existing install
  stat:
    path: "/usr/bin/batcat"
  register: batcat

- name: Install
  ansible.builtin.package:
    name:
      - bat
    state: latest
  become: true
  when: not batcat.stat.exists


